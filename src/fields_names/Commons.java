package fields_names;

public enum Commons {
	
	 // Common
     IP_ADDRESS,
     RESULT,
     HASHCODE,
     LIST,
     USERNAME,
     LOCAL_PORT,
     NO_ERRORS,
     ERRORS,
     INVALID,
     OFFLINE,
     SERVER_ERROR,
     MISSING_FIELD
}
