package fields_name;

public enum Services {
	 SERVICE,
     SERVICE_TYPE,
     ENCRYPT,
     REGISTER,
     LOGIN,
     CALL,
     ROOMS,
     CURRENT_ROOM, 
     GAME,
     UNKNOWN,
     AUDIO,
     POLLING,
     LOGOUT
}
